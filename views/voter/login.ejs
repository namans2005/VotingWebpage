<div class="min-h-[calc(100vh-4rem)] flex items-center justify-center">
    <div class="max-w-md w-full space-y-8 p-8">
        <div class="text-center">
            <h2 class="mt-6 text-3xl font-extrabold text-gray-900">
                Voter Login
            </h2>
            <p class="mt-2 text-sm text-gray-600">
                Enter your election key to proceed
            </p>
        </div>
        <form class="mt-8 space-y-6" action="/voter/login" method="POST">
            <div>
                <label for="electionKey" class="sr-only">Election Key</label>
                <input id="electionKey" name="electionKey" type="text" required
                       class="appearance-none rounded-md relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm"
                       placeholder="Enter Election Key">
            </div>

            <% if (typeof error !== 'undefined') { %>
                <div class="text-red-500 text-sm text-center"><%= error %></div>
            <% } %>

            <div>
                <button type="submit"
                        class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-green-600 hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500">
                    Enter Voting Area
                </button>
            </div>
        </form>
    </div>
</div>
